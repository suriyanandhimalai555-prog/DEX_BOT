# Environment files live next to each app (not in the repo root).
#
# Backend:  copy backend/.env.example  →  backend/.env
# Frontend: copy frontend/.env.example →  frontend/.env
#
# See README.md for how to run and test the full flow.
